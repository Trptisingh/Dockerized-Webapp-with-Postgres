# Dockerized-Webapp-with-Postgres
A hands-on Docker project demonstrating a multi-container setup using Flask (web app), PostgreSQL (database), Docker Volumes (persistent storage), and Docker Networks (service-to-service communication), orchestrated with Docker Compose.
